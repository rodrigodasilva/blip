import styled from "styled-components";

export const HeaderWrapper = styled.header`
  align-items: center;
  background: var(--neutral-onix);
  display: flex;
  height: 44px;
  justify-content: center;
  width: 100%;
  padding: 12px;
`;
